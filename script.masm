.named
.extern git.masm
@main
    call message
    pop_ret message
    push null
    push &message
    push null
    call git
    ret
@message
    push_ret #"Linker, imports, multiple files, .extern, renamed symbols, fixed comments, null"
    ret